'use strict';

/**
 * @file Exports all models at once
 */

module.exports = {
    article: require('./article'),
    history: require('./history'),
    stats: require('./stats'),
    set: require('./set'),
    summary: require('./summary')
};
